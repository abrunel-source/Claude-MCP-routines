# Hoisted node-linker so the bundled API (dist/apps/api/main.js) resolves all
# runtime dependencies from a single root node_modules — matching the Dockerfile
# and Vercel deployment layout.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
